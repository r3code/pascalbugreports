sdktest1 - тест проверяет компилируемость библиотеки bugreports
         - the test is to check compilabilty of the lib (FPC/Delphi compilers). 

fallbacktest1 - тест проверяет компонент TFilefallback 

screenshot - тест screenshot-а
